services:
  bot:
    image: telebot
    
    build:
      context: .
      dockerfile: Dockerfile

    container_name: telebot

    restart: unless-stopped

    env_file:
      - .env

    volumes:
      - .:/app

    working_dir: /app

    command: python bot.py